/** Friendly label for {service} in SMS — always "detail". */
export function formatServiceLabel() {
  return "detail";
}

/** French label for {detail} / {service_fr} in SMS. */
export function formatServiceLabelFr() {
  return "nettoyage";
}
